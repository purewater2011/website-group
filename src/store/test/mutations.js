export function setRefresh (state, status) {
  state.isRefresh = status
}

export function setForward (state, status) {
  state.isForward = status
}

export function setBack (state, status) {
  state.isBack = status
}
